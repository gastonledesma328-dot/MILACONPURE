services:
  - type: web
    name: m3u8-scraper
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: uvicorn api:app --host 0.0.0.0 --port 10000
    plan: free
